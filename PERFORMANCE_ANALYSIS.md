# Анализ производительности проекта

Дата анализа: 2026-01-11

## 🔴 Критичные проблемы (требуют немедленного внимания)

### 1. Кеширование Laravel на продакшене

**Проблема:** Кеширование конфигурации, маршрутов и представлений не включено.

**Решение:** Добавить в процесс деплоя:
```bash
php8.3 artisan config:cache
php8.3 artisan route:cache
php8.3 artisan view:cache
php8.3 artisan optimize
```

**Ожидаемый эффект:** Ускорение загрузки страниц на 30-50%

### 2. Множественные запросы к БД в DashboardController

**Проблема:** В `DashboardController::index()` выполняется 5 отдельных запросов `count()` для статистики:
- `$user->materials()->count()`
- `$user->materials()->where('moderation_status', 'approved')->count()`
- `$user->materials()->where('moderation_status', 'pending')->count()`
- `$user->materials()->where('moderation_status', 'rejected')->count()`
- `$user->authors()->count()`

**Решение:** Использовать один запрос с группировкой или кеширование:
```php
// Вариант 1: Один запрос с группировкой
$materialsStats = $user->materials()
    ->selectRaw('moderation_status, count(*) as count')
    ->groupBy('moderation_status')
    ->pluck('count', 'moderation_status')
    ->toArray();

$stats = [
    'materials_total' => array_sum($materialsStats),
    'materials_approved' => $materialsStats['approved'] ?? 0,
    'materials_pending' => $materialsStats['pending'] ?? 0,
    'materials_rejected' => $materialsStats['rejected'] ?? 0,
    'authors_count' => $user->authors()->count(),
];

// Вариант 2: Кеширование на 5 минут
$stats = Cache::remember("user_stats_{$user->id}", 300, function() use ($user) {
    // ... вычисление статистики
});
```

**Ожидаемый эффект:** Сокращение запросов к БД с 5 до 1-2, ускорение на 50-70%

### 3. Отсутствие HTTP кеширования статических файлов

**Проблема:** В `.htaccess` нет настроек для кеширования статических файлов и сжатия gzip.

**Решение:** Добавить в `public/.htaccess`:
```apache
# Кеширование статических файлов
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/json "access plus 0 seconds"
</IfModule>

# Сжатие gzip
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Кеширование браузера
<IfModule mod_headers.c>
    <FilesMatch "\.(jpg|jpeg|png|gif|webp|css|js)$">
        Header set Cache-Control "max-age=31536000, public"
    </FilesMatch>
</IfModule>
```

**Ожидаемый эффект:** Сокращение размера ответов на 60-80% (gzip), уменьшение количества запросов к серверу

## 🟡 Важные оптимизации

### 4. Удаление неиспользуемых файлов TinyMCE

**Проблема:** В `public/js/tinymce/` находится ~2.5 MB файлов, которые не используются (заменены на Quill.js).

**Решение:** Удалить директорию `public/js/tinymce/`

**Ожидаемый эффект:** Освобождение ~2.5 MB на диске, ускорение индексации директории

### 5. Множественные запросы в ModerationStatusWidget

**Проблема:** В `ModerationStatusWidget` выполняется 4 отдельных запроса `count()`.

**Решение:** Использовать один запрос или кеширование (например, на 1-2 минуты).

**Ожидаемый эффект:** Сокращение запросов к БД при загрузке админ-панели

### 6. Оптимизация запросов в MaterialController::index()

**Текущее состояние:** ✅ Используется eager loading: `->with(['type', 'authors'])`

**Проверка:** В view используется `$material->authors->pluck('name')` - это работает корректно благодаря eager loading.

**Статус:** ✅ Оптимизировано правильно

## 🟢 Рекомендации (можно реализовать позже)

### 7. Lazy loading изображений

Добавить `loading="lazy"` для изображений в списках:
```blade
<img src="{{ $image }}" alt="{{ $alt }}" loading="lazy">
```

### 8. Оптимизация изображений

- Использовать WebP формат для современных браузеров
- Автоматическое сжатие при загрузке через Intervention Image
- Ресайз изображений до нужных размеров

### 9. Минификация ассетов через Vite

Убедиться, что в production используется минифицированная версия:
```bash
npm run build  # минифицирует CSS и JS
```

### 10. CDN для статических файлов

Рассмотреть использование CDN для CSS, JS и изображений.

## 📊 Итоговая оценка производительности

### Текущее состояние:
- ⚠️ Кеширование Laravel: **Не включено**
- ⚠️ HTTP кеширование: **Отсутствует**
- ✅ Eager loading: **Используется** (MaterialController)
- ⚠️ Оптимизация запросов: **Требует улучшения** (DashboardController)
- ⚠️ Размер ассетов: **Можно уменьшить** (удалить TinyMCE)

### После применения критичных оптимизаций:
- ✅ Кеширование Laravel: **Включено**
- ✅ HTTP кеширование: **Добавлено**
- ✅ Оптимизация запросов: **Улучшено**

**Ожидаемое общее ускорение:** 40-60%

## 📝 План действий

### Немедленно (на продакшене):
1. Добавить HTTP кеширование в `.htaccess`
2. Включить кеширование Laravel (config, route, view cache)
3. Оптимизировать DashboardController (объединить запросы или кеширование)

### В ближайшее время:
4. Удалить TinyMCE файлы
5. Оптимизировать ModerationStatusWidget

### По возможности:
6. Lazy loading изображений
7. Оптимизация изображений (WebP)
8. CDN для статических файлов
