# Анализ расхождений SEO между старым и новым сайтом

## 1. Главная (/)

**Симптом:** Совпадает только description; title и h1 помечаются как несовпадающие, хотя текст в отчёте совпадает.

**Причины:**
- Сравнение в `verify:sites-seo` идёт через `normalize()` (trim + схлопывание пробелов + **lowercase**). Если в выводе есть лишние пробелы или невидимые символы — сравнение падает.
- В шаблоне `home` в БД могли остаться пробелы/переносы (например, после правки в админке).

**Что сделано:**
- Все значения из `seo_template()` и при выводе в layout приводятся через `trim()`, чтобы не было расхождений из‑за пробелов.

**Проверка:** В БД в `seo_templates` для slug `home` должны быть без лишних пробелов:
- title: `Рингтоны звонки и мелодии на телефон`
- h1: `Рингтоны на телефон`
- description: с подстановкой `%year%`.

---

## 2. Страница материала (/play/super-rington-klassika-privet-morrikone.html)

**Симптом:** На старом сайте — свой title/description («Скачать рингтон супер рингтон - классика. привет морриконе 😜»), на новом — из шаблона («Рингтон Супер рингтон - Классика. Привет Морриконе на звонок телефона…»).

**Причина:** В новой БД у материала с `slug = super-rington-klassika-privet-morrikone` поля `title` и `description` пустые (null). Мы когда‑то обнулили SEO у всех материалов; персональное SEO подтягивается только командой `import:personal-seo` из старой БД. Если её не запускали или по этому slug не нашли запись в старой `seo` (ITE), то на новом сайте подставляется шаблон.

**Что делать:**
1. Запустить подтяжку персонального SEO из старой БД:
   ```bash
   php artisan import:personal-seo --materials
   ```
2. Убедиться, что в старой БД у рингтона с alias `super-rington-klassika-privet-morrikone` есть запись в таблице `seo` (seo_type='ITE', seo_item=id рингтона) с непустыми `seo_title`/`seo_description`.
3. После синхронизации новый сайт будет брать title/description/h1 из полей материала (как на старом).

**Логика на новом сайте:** Если у материала заполнены `title` и `description` — используются они; иначе — шаблон `material` с подстановками.

---

## 3. Статическая страница (/page/programma-dlja-sozdanija-ringtonov.html)

**Симптом:** Старый h1: «Программы для нарезки ринтонов и редактирования mp3», новый: «Программа для создания рингтонов».

**Причина:** На старом сайте у страницы задан свой `seo_h1`. На новом либо не запускали подтяжку персонального SEO для страниц, либо страница не сопоставилась со старой (другой slug/page_id). Тогда в новом проекте выводится h1 из шаблона (`%page_name%` = название страницы) или из поля `page.h1`, которое ещё не обновлено из старой БД.

**Что делать:**
1. Запустить синхронизацию персонального SEO для страниц:
   ```bash
   php artisan import:personal-seo --pages
   ```
2. В старой БД должна быть запись в `seo` (seo_type='PAG', seo_item=page_id) с нужным `seo_h1`.
3. В новой БД страница должна иметь `slug = programma-dlja-sozdanija-ringtonov` (как `page_alias` на старом сайте), чтобы команда нашла её и обновила `title`, `description`, `h1`.

**Логика на новом сайте:** Если у страницы заполнены `title`, `description`, `h1` — используются они; иначе — шаблон `page` с подстановками.

---

## Итог

| Страница | В чём расхождение | Действие |
|----------|-------------------|----------|
| Главная | title/h1 (часто из‑за пробелов/нормализации) | Trim при выводе; проверить запись в `seo_templates` для `home`. |
| Материал | Используется шаблон вместо персонального SEO | Запустить `import:personal-seo --materials`. |
| Статическая страница | Другой h1 (нет персонального из старой БД) | Запустить `import:personal-seo --pages`. |

После выполнения команд и при необходимости правки шаблона главной перезапустите проверку:
```bash
php artisan verify:sites-seo --out=storage/seo-report.md
```
