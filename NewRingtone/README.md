# 🎵 Neurozvuk (Ringtone)

Laravel-приложение для работы с рингтонами и аудио материалами.

## 🚀 Быстрый старт

### Запуск сервера разработки

**Самый простой способ:**
```cmd
START.cmd
```
или дважды кликните на файл `START.cmd` в проводнике.

Сервер запустится на **http://localhost:3000**

### Остановка сервера
Нажмите `Ctrl+C` в терминале или запустите:
```cmd
STOP_SERVER.cmd
```

---

## 📋 Требования

- PHP 8.2+
- MySQL 8.0+
- Node.js 18+
- Composer
- Git

---

## ⚙️ Установка и настройка

### 1. Клонирование репозитория
```bash
git clone https://github.com/ssnikolas/Ringtone.git
cd Ringtone
```

### 2. Установка зависимостей
```bash
composer install
npm install
```

### 3. Настройка окружения
Скопируйте `.env.example` в `.env` и настройте параметры:
```bash
cp .env.example .env
```

Основные параметры в `.env`:
```env
APP_NAME=Neurozvuk
APP_URL=http://localhost:3000

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_database_user
DB_PASSWORD=your_database_password
```

### 4. Генерация ключа приложения
```bash
php artisan key:generate
```

### 5. Миграции базы данных
```bash
php artisan migrate
```

### 6. Создание символической ссылки для storage
```bash
php artisan storage:link
```

### 7. Сборка frontend
```bash
npm run build
```

---

## 🗂️ Структура проекта

```
Ringtone/
├── app/
│   ├── Filament/        # Админ-панель Filament
│   ├── Http/
│   │   └── Controllers/ # Контроллеры приложения
│   ├── Models/          # Eloquent модели
│   ├── Observers/       # Наблюдатели за моделями
│   └── Policies/        # Политики доступа
├── config/              # Конфигурационные файлы
├── database/
│   ├── migrations/      # Миграции БД
│   └── seeders/         # Сидеры
├── public/              # Публичная директория
│   └── storage/         # Символическая ссылка на storage
├── resources/
│   ├── css/             # Стили
│   ├── js/              # JavaScript
│   └── views/           # Blade шаблоны
├── routes/              # Маршруты
├── storage/
│   └── app/
│       └── public/
│           └── mp4/     # Аудио файлы
└── tests/               # Тесты
```

---

## 🎵 Аудио файлы

Аудио файлы хранятся в `storage/app/public/mp4/`.
После запуска `php artisan storage:link` они доступны по URL: `/storage/mp4/{filename}`

---

## 📊 База данных

### Основные таблицы:
- `materials` - Аудио материалы
- `types` - Типы материалов
- `categories` - Категории
- `authors` - Авторы
- `tags` - Теги
- `articles` - Статьи
- `users` - Пользователи

---

## 🔧 Полезные команды

### Очистка кеша
```bash
php artisan cache:clear
php artisan config:clear
php artisan view:clear
```

### Запуск сервера
```bash
php artisan serve --port=3000
# или просто
php -S localhost:3000 -t public
```

### Статус миграций
```bash
php artisan migrate:status
```

---

## 🛠️ Технологии

- **Backend:** Laravel 12.x
- **Admin Panel:** Filament 4.x
- **Frontend:** Tailwind CSS 4.x, Alpine.js
- **Build:** Vite 7.x
- **Database:** MySQL 8.0

---

## 📝 Changelog

### [Latest] - 2025-12-29
- Исправлена проверка существования аудио файлов в `Material::hasFile()`
- Добавлены проверки в view для предотвращения ошибок при отсутствии файлов
- Обновлен `.gitignore` для исключения временных скриптов разработки
- Добавлен `package-lock.json` в репозиторий

---

## 👥 Авторы

- GitHub: [@ssnikolas](https://github.com/ssnikolas)

---

## 📄 Лицензия

MIT License
