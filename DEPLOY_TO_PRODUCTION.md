# 🚀 Деплой на продакшен neurozvuk.ru

## 📋 Информация о сервере

- **Домен:** neurozvuk.ru
- **Путь к проекту:** ~/neurozvuk.ru/laravel/
- **Метод деплоя:** git pull через SSH

---

## 🔄 Шаги деплоя

### 1. Подключение к серверу по SSH

```bash
ssh user@neurozvuk.ru
```

### 2. Перейти в папку проекта

```bash
cd ~/neurozvuk.ru/laravel/
```

### 3. Проверить текущую версию

```bash
git log -1 --oneline
```

Должно быть: `34d1b83` (старая версия)

### 4. Получить обновления с GitHub

```bash
git fetch origin
git status
```

### 5. Выполнить git pull

```bash
git pull origin master
```

Должно подтянуть коммит `2d89ed9`

### 6. Очистить кеш и закешировать конфиг/маршруты/шаблоны (продакшен)

После обновления кода на продакшене:

```bash
# Очистить старый кеш
php8.3 artisan cache:clear
php8.3 artisan config:clear
php8.3 artisan view:clear
php8.3 artisan route:clear

# Закешировать конфиг, маршруты и шаблоны (ускоряет загрузку страниц)
php8.3 artisan config:cache
php8.3 artisan route:cache
php8.3 artisan view:cache
# или одной командой:
php8.3 artisan optimize
```

⚠️ **Важно:** На сервере используется PHP 8.3, поэтому команды должны быть с `php8.3`

### 7. Проверить, что всё работает

```bash
php artisan migrate:status
```

### 8. Перезапустить PHP-FPM или веб-сервер (если нужно)

```bash
# Для Nginx + PHP-FPM:
sudo systemctl reload php-fpm
sudo systemctl reload nginx

# Или для Apache:
sudo systemctl reload apache2
```

---

## ✅ Что изменится на продакшене:

1. **Главная страница** - в блоках типов будет по 6 материалов (было 3)
2. **Меню Header** - "Звуки" → "Мелодии", удалены "Статьи"
3. **Мобильное меню** - "Звуки" → "Мелодии", удалены "Цитаты" и "Статьи"
4. **Footer** - "Звуки" → "Мелодии"

---

## 🔍 Проверка после деплоя:

1. Откройте https://neurozvuk.ru
2. Проверьте меню - должна быть ссылка "Мелодии" вместо "Звуки"
3. Проверьте блок "6 типов контента" - должно быть по 6 элементов
4. Проверьте мобильное меню (гамбургер)
5. Проверьте footer

---

## ⚠️ На случай проблем:

### Откатить изменения:

```bash
cd /neurozvuk.ru/laravel/
git reset --hard 34d1b83
php artisan cache:clear
```

### Посмотреть логи:

```bash
tail -f storage/logs/laravel.log
```

---

## 📝 Коммиты для деплоя:

**Коммит 1:** `34d1b83` → `2d89ed9`

**Изменения:**
- MainController.php (3→6 материалов)
- Header, Mobile menu, Footer (обновлены ссылки)

---

## 🎯 Быстрая команда (всё в одной строке):

```bash
cd /neurozvuk.ru/laravel/ && git pull origin master && php artisan cache:clear && php artisan config:clear && php artisan view:clear && echo "✓ Deploy complete!"
```

---

**Готово к деплою!** 🚀

